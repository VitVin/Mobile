import { StyleSheet } from "react-native";
export const Style = StyleSheet.create({
    text: {
     marginTop: 10,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
     fontSize: 15,
     color: "white"
  }});
  